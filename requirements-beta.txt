# Runtime and packaging dependencies for the Windows beta.
# Whisper/torch are intentionally omitted from the one-file beta; lyric sync
# remains optional and the source install can provide that heavier stack.
numpy>=1.26
scipy>=1.11
librosa>=0.10.2
soundfile>=0.12.1
numba>=0.59
llvmlite>=0.42
imageio>=2.34
imageio-ffmpeg>=0.5
Pillow>=10.2
PyYAML>=6.0
requests>=2.34.2
pyinstaller>=6.10
pytest>=8.0
